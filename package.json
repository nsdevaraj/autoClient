{
  "name": "autodraw-client",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone AutoDraw web client: sketch on a canvas and swap strokes for icon suggestions from local ONNX models.",
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test"
  }
}
